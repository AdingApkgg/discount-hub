# Web app URL the Tauri shell loads in its WebView.
# Dev: defaults to http://localhost:3000 (start web dev server first)
# Prod: set to your deployed domain, e.g. https://your-domain.com
DISCOUNT_HUB_WEB_URL=http://localhost:3000
